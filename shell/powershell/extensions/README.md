# Powershell Extensions Directory

Populate it with local shell scripts and variables that are meant to be auto-included and ignored by git

## Example scripts

- `aliases.ps1`: local aliases
- `functions.ps1`: local functions
